fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3

# LLM (OpenAI, or Gemini through its OpenAI-compatible endpoint)
openai==3.19.2

# Web search + dead-link checks
ddgs==9.16.0
httpx==0.28.1

# Supabase JWT verification (ES256 via JWKS needs cryptography)
PyJWT[crypto]==2.15.0
cryptography==50.0.1
